const SESSION_TIMEOUT_MS=30 * 60 * 1000;// 30 minutes(GA4 default)function getOrCreateSessionId(){const stored=sessionStorage.getItem('ga_session_data');const now=Date.now();if(stored){const data=JSON.parse(stored);const timeSinceLastActivity=now - data.lastActivityTime;if(timeSinceLastActivity < SESSION_TIMEOUT_MS){data.lastActivityTime=now;sessionStorage.setItem('ga_session_data',JSON.stringify(data));return{sessionId:data.sessionId,sessionNumber:data.sessionNumber,sessionStartTime:data.sessionStartTime,isNewSession:false};}}const sessionData={sessionId:`${Date.now()}_${Math.random().toString(36).substr(2,9)}`,sessionNumber:stored?JSON.parse(stored).sessionNumber + 1:1,sessionStartTime:now,lastActivityTime:now};sessionStorage.setItem('ga_session_data',JSON.stringify(sessionData));return{sessionId:sessionData.sessionId,sessionNumber:sessionData.sessionNumber,sessionStartTime:sessionData.sessionStartTime,isNewSession:true};}function getEngagementTime(sessionStartTime){return Date.now()- sessionStartTime;}function collectUserProperties(){return{language:navigator.language || 'unknown',screen_resolution:`${screen.width}x${screen.height}`,viewport_size:`${window.innerWidth}x${window.innerHeight}`,user_agent:navigator.userAgent,platform:navigator.platform || 'unknown'};}window.sendGAEvent=function(eventName,parameters){console.log("[GA4 Proxy]===Event Tracking Start===");console.log("[GA4 Proxy] Event:",eventName);console.log("[GA4 Proxy] Parameters:",parameters);let clientId=localStorage.getItem('ga_client_id');if(!clientId){clientId=crypto.randomUUID();localStorage.setItem('ga_client_id',clientId);console.log("[GA4 Proxy] Generated new client ID:",clientId);}const session=getOrCreateSessionId();console.log("[GA4 Proxy] Session ID:",session.sessionId);console.log("[GA4 Proxy] Session Number:",session.sessionNumber);console.log("[GA4 Proxy] New Session:",session.isNewSession);const engagementTime=getEngagementTime(session.sessionStartTime);console.log("[GA4 Proxy] Engagement Time:",engagementTime,"ms");const userProps=collectUserProperties();const payload={eventName:eventName,parameters:{...(parameters ||{}),session_id:session.sessionId,engagement_time_msec:engagementTime,session_number:session.sessionNumber,language:userProps.language,screen_resolution:userProps.screen_resolution,viewport_size:userProps.viewport_size},clientId:clientId,timestamp:new Date().toISOString()};const proxyUrl='https://analytics.mekanika.org';if(session.isNewSession && eventName !=='session_start'){console.log("[GA4 Proxy] 🆕 Sending session_start event(new session detected)");const sessionStartPayload={eventName:'session_start',parameters:{session_id:session.sessionId,engagement_time_msec:0,session_number:session.sessionNumber,language:userProps.language,screen_resolution:userProps.screen_resolution,viewport_size:userProps.viewport_size},clientId:clientId,timestamp:new Date().toISOString()};fetch(proxyUrl,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(sessionStartPayload),keepalive:true}).catch(err=> console.error("[GA4 Proxy] session_start failed:",err));const stored=JSON.parse(sessionStorage.getItem('ga_session_data'));stored.isNewSession=false;sessionStorage.setItem('ga_session_data',JSON.stringify(stored));}console.log("[GA4 Proxy] Sending to proxy:",proxyUrl);console.log("[GA4 Proxy] Full payload:",payload);fetch(proxyUrl,{method:'POST',headers:{'Content-Type':'application/json',},body:JSON.stringify(payload),keepalive:true // Ensures request completes even if page unloads}).then(response=>{if(!response.ok){throw new Error(`HTTP ${response.status}`);}return response.json();}).then(data=>{console.log("[GA4 Proxy] ✅ Event sent successfully:",data);return true;}).catch(error=>{console.error("[GA4 Proxy] ❌ Event failed:",error);return false;});return true;};window.getSessionId=function(){const session=getOrCreateSessionId();return session.sessionId;};window.checkProxyStatus=function(){const proxyUrl='https://analytics.mekanika.org';console.log("===PROXY STATUS CHECK===");console.log("Testing connection to:",proxyUrl);fetch(proxyUrl,{method:'OPTIONS',headers:{'Content-Type':'application/json',}}).then(response=>{console.log("✅ Proxy is reachable. Status:",response.status);console.log("CORS headers:",response.headers.get('Access-Control-Allow-Origin'));const session=getOrCreateSessionId();console.log("📊 Current Session ID:",session.sessionId);console.log("📊 Session Number:",session.sessionNumber);console.log("📊 Session Age:",Math.floor((Date.now()- session.sessionStartTime)/ 1000),"seconds");return true;}).catch(error=>{console.error("❌ Proxy not reachable:",error);console.error("Make sure Cloudflare Worker is deployed and CORS is configured");return false;});};window.addEventListener('load',()=>{setTimeout(()=>{console.log("[GA4 Proxy] Page loaded,checking proxy status...");window.checkProxyStatus();},1000);});